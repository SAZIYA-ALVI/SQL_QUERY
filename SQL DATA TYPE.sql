--SQL DATA TYPE
-- String data
-- char , varchar,nchar,nvarchar

-- M or F , char(1)
-- varchar(256)
-- nchar(1), it support unicode
-- nvarchar(256), it supports unicode

-- number type
-- int, bignit,tinyint, decimal(p,s),smallint

-- age of the students , tinyint
-- age of trees/buildings , smallint

-- decimal(p,s) p represent total digits, s refers to number of digit you may have after decimal

-- 999.99, decimal(10,5)

-- date data type, time data type, datetime data type
